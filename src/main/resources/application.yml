spring:
  config:
    import: application-mysql.yml, application-flask.yml
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
springdoc:
  swagger-ui:
    operations-sorter: method
